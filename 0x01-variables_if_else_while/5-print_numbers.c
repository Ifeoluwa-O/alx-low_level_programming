#include <stdio.h>

/* prints all single digit numbers of base 10 */

/**
 * main - entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	int i = 0;

	for (i <= 9; i++)
		printf("%d", i)
	printf("\n");
	return (0);
}
