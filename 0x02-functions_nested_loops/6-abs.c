#include "main.h"

/**
 * _abs - a function that returns absolute value
 * @int: integer
 * Return: absolute vale of variable
 */

int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
