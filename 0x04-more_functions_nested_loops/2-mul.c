#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: variable number
 * @b: variable number
 * Return: result= an integer
 */

int mul(int a, int b)
{
	result = a * b;
		return (result);
}
